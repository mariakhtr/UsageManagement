
1. Create a new project
2. Create a model(database) through generate
3. Create a controller, generate -> Controller 
4. Add the attributes of the created table.
5. Rake Task -> migrate db
6. Add html.erb file to views.
